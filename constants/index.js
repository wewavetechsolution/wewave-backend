const tables = {
    tablebanner: "banner_content"
}

const db_connection = {
    host: 'localhost',
    user: 'root',
    password: 'Pupundey@123',
    database: 'wewave'
}

const PORT = 4000

module.exports = { tables, db_connection, PORT  }